const DEFAULT = `var(--default);`;
const DEFAULT_BG = `var(--default-bg);`;
const DEFAULT_PAD = `var(--default-pad);`;
const DEFAULT_FONT = `var(--default-font);`;
const DEFAULT_SIZE = `var(--default-size);`;
const DEFAULT_COLOR = `var(--default-color);`;

export {
  DEFAULT,
  DEFAULT_BG,
  DEFAULT_PAD,
  DEFAULT_FONT,
  DEFAULT_SIZE,
  DEFAULT_COLOR
};